name = "trysdk-preview-proxy"
main = "src/index.ts"
compatibility_date = "2026-08-30"

# Add a wildcard custom domain after deployment, for example:
# routes = [{ pattern = "*.preview.example.com/*", zone_name = "example.com" }]
# The first hostname label must be a Try SDK job ID.
